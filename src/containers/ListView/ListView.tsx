import React from "react";
import styled from "styled-components";

const ListViewWrapper = styled.div``;

const ListView = () => {
  return <ListViewWrapper></ListViewWrapper>;
};

export default ListView;
