import React from "react";
import { render } from "@testing-library/react";
import map from "./map";

describe("map 컴포넌트", () => {
  it("import시 정상적으로 사용 가능하지를 테스트한다", () => {
    // const map = render(<map />);
  });
  it("컴포넌트 Render시 Header에 동적으로 script tag가 load되는지를 확인한다.", () => {
  })
  it("Rneder시 Naver Map이 정상적으로 호출 되었는지를 확인한다.", () => {
  })
  it("컴포넌트 초기값 설정이 정상적으로 작동하는지 확인한다.", () => {
  })
  it("사용자값이 컴포넌트에 적용 되었는지 확인한다.", () => {
  })
  it("Naver Map의 Maker가 표시되는지 확인한다.", () => {
  })
  it("지도 level별로 클러스터링이 되는지 확인한다.", () => {
  })
  it("최하 level에서 매물정보가 보이는지 확인한다.", () => {
  })
});
