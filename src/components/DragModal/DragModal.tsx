import React from 'react';
import styled from 'styled-components';

const DragModalWrapper = styled.div`

`

const DragModal = () => {
    return (
      <DragModalWrapper>

      </DragModalWrapper>
    )
  }
  
export default DragModal;